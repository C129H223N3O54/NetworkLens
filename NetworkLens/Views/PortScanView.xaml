<UserControl x:Class="NetworkLens.Views.PortScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:NetworkLens.Localization"
             Background="{DynamicResource BgMainBrush}"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="28,24,28,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding [Port_Title], Source={x:Static loc:LocalizationManager.Instance}}" Style="{StaticResource H1}"/>
                    <TextBlock Text="{Binding [Port_FullSubtitle], Source={x:Static loc:LocalizationManager.Instance}}"
                               Margin="0,4,0,0" Foreground="{DynamicResource TextSecondaryBrush}"
                               FontFamily="Verdana" FontSize="13"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnStop" Content="{Binding [Monitor_StopButton], Source={x:Static loc:LocalizationManager.Instance}}" Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" IsEnabled="False" Click="BtnStop_Click"/>
                    <Button x:Name="BtnScan" Content="{Binding [Port_StartScan], Source={x:Static loc:LocalizationManager.Instance}}" Style="{StaticResource PrimaryButton}"
                            Click="BtnScan_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Config Bar -->
        <Border Grid.Row="1" Margin="28,0,28,12">
            <Border Style="{StaticResource CardBorder}" Padding="16,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding [Port_TargetIp], Source={x:Static loc:LocalizationManager.Instance}}" VerticalAlignment="Center"
                               FontFamily="Verdana" FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" x:Name="TxtTargetIp"
                             FontFamily="Consolas" FontSize="13"/>

                    <TextBlock Grid.Column="3" Text="{Binding [Port_ProfileLabel], Source={x:Static loc:LocalizationManager.Instance}}" VerticalAlignment="Center"
                               FontFamily="Verdana" FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="4" x:Name="CmbProfile"
                              SelectionChanged="CmbProfile_SelectionChanged">
                        <ComboBoxItem Content="⚡ Quick (Top 20)"/>
                        <ComboBoxItem Content="📋 Standard (Top 100)" IsSelected="True"/>
                        <ComboBoxItem Content="🔍 Full (1–65535)"/>
                        <ComboBoxItem Content="✏ Custom"/>
                    </ComboBox>

                    <TextBlock Grid.Column="6" Text="Ports:" VerticalAlignment="Center"
                               FontFamily="Verdana" FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="20,0,10,0" x:Name="LblCustomPorts" Visibility="Collapsed"/>
                    <TextBox Grid.Column="7" x:Name="TxtCustomPorts"
                             Text="80,443,8080,22,3389" Visibility="Collapsed"
                             FontFamily="Consolas" FontSize="12"
                             ToolTip="Kommagetrennt: 80,443 oder Bereich: 8000-8100"/>
                </Grid>
            </Border>
        </Border>

        <!-- Progress -->
        <Border Grid.Row="2" x:Name="ProgressPanel" Margin="28,0,28,12"
                Style="{StaticResource CardBorder}" Padding="16,12" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock x:Name="TxtProgressLabel" Text="Scanne …"
                               FontFamily="Verdana" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="TxtProgressPct" HorizontalAlignment="Right"
                               FontFamily="Verdana" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}"/>
                </Grid>
                <ProgressBar Grid.Row="1" x:Name="ScanProgress" Height="6" Value="0" Maximum="100"/>
                <TextBlock Grid.Row="2" x:Name="TxtProgressDetail" Margin="0,6,0,0"
                           FontFamily="Verdana" FontSize="11.5"
                           Foreground="{DynamicResource TextMutedBrush}"/>
            </Grid>
        </Border>

        <!-- Results -->
        <Border Grid.Row="3" Margin="28,0,28,24" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Grid Grid.Row="0" Margin="16,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtOpenCount" Text="—"
                                   FontFamily="Verdana" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <!-- Filter pills -->
                        <Border x:Name="PillAll" Margin="12,0,0,0" CornerRadius="12" Padding="10,3"
                                Background="{DynamicResource AccentBrush}" Cursor="Hand">
                            <TextBlock Text="Alle" FontSize="11" FontWeight="Bold" Foreground="#FF000000"/>
                        </Border>
                        <Border x:Name="PillOpen" Margin="6,0,0,0" CornerRadius="12" Padding="10,3"
                                Background="{DynamicResource BgDeepBrush}" Cursor="Hand"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <TextBlock Text="Offen" FontSize="11"
                                       Foreground="{DynamicResource StatusOnlineBrush}"/>
                        </Border>
                        <Border x:Name="PillFiltered" Margin="6,0,0,0" CornerRadius="12" Padding="10,3"
                                Background="{DynamicResource BgDeepBrush}" Cursor="Hand"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <TextBlock Text="Gefiltert" FontSize="11"
                                       Foreground="{DynamicResource StatusWarningBrush}"/>
                        </Border>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Content="{Binding [Port_CopyAsText], Source={x:Static loc:LocalizationManager.Instance}}"
                            Style="{StaticResource SecondaryButton}" Padding="10,5" FontSize="12"
                            Click="BtnCopyResults_Click"/>
                </Grid>

                <Border Grid.Row="1" Height="1" Background="{DynamicResource BorderBrush}"/>

                <!-- Empty state -->
                <StackPanel x:Name="EmptyState" Grid.Row="2"
                            VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⬡" FontSize="48" Foreground="{DynamicResource TextMutedBrush}"
                               HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="{Binding [Port_NoPortScan], Source={x:Static loc:LocalizationManager.Instance}}"
                               FontFamily="Verdana" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding [Port_NoPortScanHint], Source={x:Static loc:LocalizationManager.Instance}}"
                               FontFamily="Verdana" FontSize="12"
                               Foreground="{DynamicResource TextMutedBrush}"
                               HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Port grid -->
                <DataGrid Grid.Row="2" x:Name="PortGrid"
                          IsReadOnly="True" CanUserSortColumns="True"
                          Visibility="Collapsed">
                    <DataGrid.Columns>
                        <!-- Status dot -->
                        <DataGridTemplateColumn Header="●" Width="44" SortMemberPath="Status">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}"
                                             HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Port -->
                        <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="80"
                                           SortMemberPath="Port">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Status Text -->
                        <DataGridTemplateColumn Header="Status" Width="100" SortMemberPath="StatusText">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="7,2"
                                            Background="{Binding StatusBgColor}"
                                            HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusText}" FontSize="11"
                                                   FontWeight="Bold" Foreground="{Binding StatusColor}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Service -->
                        <DataGridTextColumn Header="Dienst" Binding="{Binding DisplayService}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Verdana"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Banner -->
                        <DataGridTextColumn Header="Banner / Antwort" Binding="{Binding Banner}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="11.5"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Ping -->
                        <DataGridTextColumn Header="ms" Width="60" SortMemberPath="ResponseTime">
                            <DataGridTextColumn.Binding>
                                <Binding Path="ResponseTime" StringFormat="{}{0}"/>
                            </DataGridTextColumn.Binding>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
